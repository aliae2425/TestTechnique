{% extends 'base.html.twig' %}

{% block title %}Tableau de bord - 418Academy{% endblock %}

{% block body %}
    <div class="flex min-h-[calc(100vh-80px)] bg-slate-50/50 mt-[72px]">
        {# Sidebar Filtres #}
        <aside class="w-64 flex-shrink-0 border-r border-slate-200 bg-white/50 backdrop-blur-sm p-6 hidden lg:block fixed h-full overflow-y-auto">
            <div class="flex items-center justify-between mb-6">
                <h2 class="font-bold text-slate-800">Filtres</h2>
                <button class="text-xs text-cyan-600 hover:text-cyan-700 font-medium">Réinitialiser</button>
            </div>

            <div class="space-y-8">
                {# Section Difficulté #}
                <div>
                    <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-3">Difficulté</h3>
                    <div class="space-y-2">
                        {% for level in ['Débutant', 'Intermédiaire', 'Avancé', 'Expert'] %}
                            <label class="flex items-center gap-3 cursor-pointer group">
                                <div class="relative flex items-center">
                                    <input type="checkbox" class="peer h-4 w-4 rounded border-slate-300 text-cyan-600 focus:ring-cyan-500/20">
                                </div>
                                <span class="text-sm text-slate-600 group-hover:text-slate-900 transition-colors">{{ level }}</span>
                            </label>
                        {% endfor %}
                    </div>
                </div>

                {# Section Catégories #}
                <div>
                    <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-3">Catégories</h3>
                    <div class="space-y-2">
                        {% for cat in ['Modélisation', 'Documentation', 'Coordination', 'Familles', 'Plugins'] %}
                            <label class="flex items-center gap-3 cursor-pointer group">
                                <input type="checkbox" class="h-4 w-4 rounded border-slate-300 text-cyan-600 focus:ring-cyan-500/20">
                                <span class="text-sm text-slate-600 group-hover:text-slate-900 transition-colors">{{ cat }}</span>
                            </label>
                        {% endfor %}
                    </div>
                </div>

                {# Section Tags #}
                <div>
                    <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-3">Tags Populaires</h3>
                    <div class="flex flex-wrap gap-2">
                        {% for tag in ['modélisation', 'documentation', 'coordination', 'familles', 'plugins'] %}
                            <button class="px-2 py-1 rounded-md bg-slate-100 hover:bg-slate-200 text-xs font-medium text-slate-600 transition-colors border border-transparent hover:border-slate-300">
                                #{{ tag }}
                            </button>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </aside>

        {# Main Content #}
        <main class="flex-1 lg:ml-64 p-6 lg:p-10">
            {# Search Bar #}
            <div class="max-w-2xl mx-auto mb-10">
                <div class="relative group">
                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-slate-400 group-focus-within:text-cyan-500 transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                        </svg>
                    </div>
                    <input type="text" 
                           class="block w-full pl-11 pr-4 py-3 rounded-xl border-slate-200 bg-white shadow-sm focus:border-cyan-500 focus:ring-4 focus:ring-cyan-500/10 placeholder-slate-400 transition-all text-sm" 
                           placeholder="Rechercher un quiz, une compétence, un sujet...">
                    <div class="absolute inset-y-0 right-0 pr-3 flex items-center">
                        <kbd class="hidden sm:inline-flex items-center gap-1 px-2 py-1 rounded border border-slate-200 bg-slate-50 text-[10px] font-medium text-slate-500">
                            <span class="text-xs">⌘</span>K
                        </kbd>
                    </div>
                </div>
            </div>

            {# Grid de Cartes #}
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                {% for i in 1..8 %}
                <div class="group bg-white rounded-2xl border border-slate-100 shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all duration-300 flex flex-col h-full overflow-hidden">
                    <div class="flex h-32 p-4 gap-4">
                        {# Image Côté Gauche #}
                        <div class="w-1/3 min-w-[33%] h-full rounded-xl bg-gradient-to-br from-slate-100 to-slate-200 overflow-hidden relative">
                             {# Placeholder pour l'image #}
                             <div class="absolute inset-0 flex items-center justify-center text-2xl">
                                {{ ['🐘', '⚛️', '🐍', '☕'][random(0, 3)] }}
                             </div>
                        </div>
                        
                        {# Titre et Description Partie Base (Côté Droit ici pour l'agencement flex row puis col) #}
                        {# En respectant "Titre + description en partie base", on suppose ici le contenu texte à droite de l'image ou en dessous si on veut vraiment "partie base". 
                           Structure demandée: "image coté gauche avec un titre une description en partie base".
                           Interprétation: 
                           Haut: Row (Img Left, Title Right)
                           Bas: Description ?
                           Ou tout vertical: Img Left? (Impossible en grid vertical).
                           
                           Voici une structure hybride propre:
                           Row Top: Image carrée Left | Title Right
                           Row Bottom: Description
                        #}
                        <div class="flex flex-col flex-1 gap-1">
                            <div class="flex items-start justify-between">
                                <h3 class="font-bold text-slate-800 leading-tight group-hover:text-cyan-600 transition-colors line-clamp-2">
                                    Cours revit {{ i }}
                                </h3>
                            </div>
                            <span class="text-xs font-semibold text-cyan-600 bg-cyan-50 px-2 py-0.5 rounded-full w-fit">
                                Intermédiaire
                            </span>
                        </div>
                    </div>

                    <div class="px-4 pb-2">
                        <p class="text-sm text-slate-500 line-clamp-2 mb-3">
                            Testez vos connaissances sur les composants Symfony et l'injection de dépendances.
                        </p>
                    </div>

                    <div class="mt-auto">
                        {# Séparateur #}
                        <div class="h-px bg-slate-50 mx-4"></div>
                        
                        {# Liste de Tag sous un séparateur #}
                        <div class="px-4 py-3 bg-slate-50/50 flex flex-wrap gap-2 items-center">
                            <span class="text-[10px] uppercase font-bold text-slate-400 tracking-wider">Tags</span>
                            <div class="flex gap-1">
                                <span class="bg-white border border-slate-100 text-slate-600 text-[10px] px-1.5 py-0.5 rounded shadow-sm">#sf6</span>
                                <span class="bg-white border border-slate-100 text-slate-600 text-[10px] px-1.5 py-0.5 rounded shadow-sm">#services</span>
                            </div>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </main>
    </div>
{% endblock %}
